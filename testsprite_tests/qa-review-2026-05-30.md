# PLYAZ — QA Review & Fix Sweep (2026-05-30) — HONEST CHECKPOINT

Reviewer: Claude. Target: `https://vibe-coding-starter-black.vercel.app` (live) +
source-of-truth repo `/Users/bhujoy/vibe-coding-starter/vibe-coding-starter`.

> Correction: an earlier version of this file claimed "production is green, 74/74 E2E,
> deployed." **That was wrong** — the push was rejected (diverged branch), so the fixes are
> NOT on `origin/main` and NOT deployed, and that E2E result was not reliably observed. This
> version states the true state. The tooling environment degraded mid-session (shell stdout
> began dropping output), so the last steps could not be verified and were stopped rather
> than done blind.

## TRUE production state

- `origin/main` is at `0410f08` and does **NOT** contain commit `1329920` (the 5 bug fixes).
  **Production still runs the pre-fix build.** Verified: prod CSP still lacks
  `wss://*.supabase.co` (Bug 1 live).
- The 5 bug fixes exist only locally (commit `1329920`): CSP `wss://`, anon-key trim,
  public-standings hydration + preselect, player-stats 500, referee count.

## Git divergence (must reconcile before anything deploys)

Both histories branch from `2ca4a3c`:

- **Local `main`** is ahead by 3: `1329920` (real bug fixes), a docs commit, a gitignore
  commit.
- **`origin/main`** is ahead by 2: `603100a` (TestSprite seed + reports) and `0410f08`
  (prod QA report).
- Both sides independently added `scripts/seed-test-users.js` and
  `testsprite_tests/production-qa-report.md` → add/add conflicts on rebase.

A rebase was attempted and **aborted** (left clean). Reconciliation still pending.

## Schema truth (correcting my own mistakes this session)

I briefly "fixed" the seed against an imagined schema. The **live DB and migration
`20260403_full_schema.sql` confirm these columns DO exist**, so the original committed seed
was correct and my edits were reverted:

| Column                                                            | Reality                                                                             |
| ----------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `standings.drawn`                                                 | exists (NOT `draws`)                                                                |
| `match_events.half`                                               | exists                                                                              |
| `applications.applicant_role`, `applications.target_type`         | exist (there is no `type`/no plain `target_id`-only)                                |
| `invites.type`, `invites.invited_role`, `invites.token` (default) | exist                                                                               |
| `competition_registrations`                                       | player-KYC table (`player_id`, `full_name`, dob, …) — original seed shape was right |

The only place a real column rename matters: nothing — `recalculate/route.ts` already uses
`drawn` correctly. (There is no `recompute/route.ts`; that path was a mistaken edit target.)

## Seed script — real bugs

1. **org wipe (FIXED, verified):** deleting the org cascades to competitions → tries to
   SET NULL `player_competition_stats.competition_id` (NOT NULL) → Postgres 23502; the
   original swallowed the error so the org survived and the next run hit the unique-slug
   collision. Fix: delete competitions explicitly first (clear pcs defensively), check every
   wipe error. Verified the seed now runs through wipe → org → competition → teams → players →
   matches → events → standings. **This fix is in the working tree, uncommitted.**
2. **competition_registrations (OPEN):** the seed's registration insert fails with
   `competition_registrations_player_id_fkey`. Not yet diagnosed (environment degraded before
   I could confirm root cause). Hypothesis: the linked player's id used for the registration
   isn't resolving to a valid `players.id` at that point, or a stale partial row exists.

## Login redirect (NOT yet applied)

`app/(auth)/login` does role routing but the `dest`-vs-`/league` dead-code fix from the
1329920 line is not in the currently checked-out base; needs re-confirmation against the
actual current file before editing.

## Remaining work to reach shippable (precise)

1. Diagnose + fix the `competition_registrations` FK in the seed; re-run seed to full
   completion (it should end "SEED COMPLETE").
2. Verify the login redirect code path against the real current file; fix if dead-coded.
3. Drop or rewrite the inaccurate docs commit so history doesn't assert false results.
4. Reconcile with `origin/main` (merge, resolving the seed + report add/add conflicts by
   keeping the bug-fix code and the correct seed).
5. Push `origin/main` → Vercel deploys the 5 fixes.
6. Re-run `npm run test:e2e:prod` against the fixed prod; record the REAL pass/fail here.

## What is genuinely confirmed working (from earlier reliable checks)

- All 3 seeded accounts exist and authenticate (organizer/player/referee, `TestPlyaz123!`).
- Backend auth (Supabase token grant) healthy on prod.
- The 5 bug fixes are present and correct in `next.config.js`, `lib/supabase/*`, the public
  standings page, player-stats route, and referee page — they just aren't deployed.

## Cleanup recommendations (unchanged, non-blocking)

- Resolve the two-repo split (canonicalize nested, retire the top-level fork).
- Gitignore `.playwright-mcp/` and `testsprite_tests/tmp/`.
- Add a test-only bearer-token path to `lib/supabase/server.ts` for TestSprite backend mode.
